name: CD - DEV

on:
  push:
    branches:
      - develop

jobs:
  deploy-dev:
    runs-on: ubuntu-latest

    environment:
      name: dev

    steps:
      - name: Deploy DEV
        run: |
          echo "================================"
          echo "        DEPLOY DEV"
          echo "================================"
          echo "Commit: ${{ github.sha }}"
          echo "Ambiente: DEV"
          echo "Status: Deploy realizado com sucesso"